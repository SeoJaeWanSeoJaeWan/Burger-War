import styled from "styled-components";

const Container = styled.main``;

const Canvas = styled.canvas``;

const GameTemplateStyle = {
    Container,
    Canvas,
};

export default GameTemplateStyle;
